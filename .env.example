# Copie para .env e exporte no seu shell, ou use ferramentas que carregam .env automaticamente.
# A aplicação lê diretamente do ambiente (não depende de python-dotenv).
OPENAI_API_KEY=coloque_sua_chave_aqui

# Ajustes opcionais
# TRANSCRIBE_MODEL=gpt-4o-mini-transcribe
# TRANSCRIBE_LANGUAGE=pt
# TRANSCRIBE_FORMAT=json
# SUMMARY_MODEL=gpt-4o-mini
# SUMMARY_TEMPERATURE=0.2
# OPENAI_TIMEOUT=120
# OPENAI_MAX_RETRIES=3
